version: '3.8'

services:
  receipt-manager:
    build: .
    container_name: receipt-manager
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
      - ./.env:/app/.env
    restart: unless-stopped
